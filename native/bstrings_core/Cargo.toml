[package]
name = "bstrings_core"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
codegen-units = 1
lto = "thin"
panic = "abort"
strip = "debuginfo"
